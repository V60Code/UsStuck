{
  "name": "starter-project-with-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run prepare-deploy && vite build",
    "preview": "vite preview",
    "prepare-deploy": "node prepare-deploy.js",
    "deploy": "npm run build"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
